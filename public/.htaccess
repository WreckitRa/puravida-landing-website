# Apache configuration for Next.js SSR behind reverse proxy
# 
# Note: With SSR, Apache should proxy requests to the Next.js server.
# This .htaccess is kept for minimal redirects only.
# Most routing is handled by the Next.js server itself.

RewriteEngine On
RewriteBase /

# Prevent directory listing
Options -Indexes

# Handle /app/invite redirect
# Redirect to /ihaveinvite for desktop (mobile redirects handled server-side if needed)
RewriteRule ^app/invite/?$ /ihaveinvite [L,R=301]

# Note: With SSR, dynamic routes like /event/[id] are handled by Next.js server-side.
# No Apache rewrite rules needed for event routes.
